cmake_minimum_required(VERSION 3.0.2)
project(planner_gazebo_sim)

find_package(catkin_simple REQUIRED)

catkin_simple(ALL_DEPS_REQUIRED)

#include_directories(
#  ${Eigen_INCLUDE_DIRS})

#cs_add_executable(planner_control_interface_node src/planner_rotors.cpp)

#cs_install()
#cs_export(LIBRARIES)
